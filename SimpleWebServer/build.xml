<?xml version="1.0" encoding="UTF-8"?>
<project name="WebServer" default="compile" basedir=".">
    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="dist" location="dist"/>

    <target name="init">
        <mkdir dir="${build}"/>
        <mkdir dir="${dist}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src}" destdir="${build}">
            <include name="edu/nmsu/cs/webserver/**/*.java"/>
        </javac>
    </target>

    <target name="jar" depends="compile">
        <jar destfile="${dist}/WebServer.jar" basedir="${build}">
            <manifest>
                <attribute name="Main-Class" value="edu.nmsu.cs.webserver.WebServer"/>
            </manifest>
        </jar>
    </target>

    <target name="clean">
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
    </target>

    <target name="run" depends="jar">
        <java jar="${dist}/WebServer.jar" fork="true"/>
    </target>
</project>